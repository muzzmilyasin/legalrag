services:
  - type: web
    name: legal-rag
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn web_app:app --timeout 120 --workers 1
    envVars:
      - key: GROQ_API_KEY
        sync: false          # you'll paste this in the Render dashboard
